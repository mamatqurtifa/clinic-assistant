{
  "name": "clinic-calendar-proxy",
  "version": "2.0.0",
  "description": "Proxy backend Express.js untuk booking konsultasi dokter via Google Calendar API. Setiap user login dengan akun Google sendiri — refresh token disimpan di sisi klien, bukan di server.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.1"
  }
}
